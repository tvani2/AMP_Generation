# Optional run configs for this thesis project.
#
# These files document *which data + checkpoint* each experiment uses.
# Notebooks can still hard-code Drive paths; configs are for clarity
# (and for you/your mentor to see experiment settings in one place).
#
# How to use later (optional):
#   import yaml
#   cfg = yaml.safe_load(open("../configs/baumannii_enterica.yaml"))
